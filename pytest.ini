[pytest]
markers =
    integration: tests requiring live LLM providers

filterwarnings =
    ignore::DeprecationWarning:google.*
    ignore::DeprecationWarning:pydantic.*
    ignore:datetime.datetime.utcnow\(\) is deprecated:DeprecationWarning

testpaths = tests
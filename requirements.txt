# === CORE KERNEL ===
python-telegram-bot>=22.0

# === MEMORY LAYER ===
chromadb>=1.5.0
# pin SHA fork : la 3.3.5 PyPI ne contient PAS le commit configurable
# embedder (model_name + marker .mempalace-embedder.json) dont dépend le
# palace mpnet-768. Cf. dette #28 sprint 13, audit pin_mempalace.md §4.
# Tout bump de ce SHA doit être délibéré + tracé (validation smoke runbook §6).
mempalace @ git+https://github.com/cossonn-coder/mempalace.git@b8caf3259021d27c2689928458ac02d5a0defd01
# pin explicite : couche HNSW C++ critique pour le palace, cf. dette #27 sprint 9
chroma-hnswlib==0.7.6

# === EMBEDDINGS ===
sentence-transformers>=5.0.0
numpy>=1.26

# === LLM PROVIDERS ===
groq>=1.1.0
mistralai>=2.0.0
cerebras-cloud-sdk>=1.60.0
google-generativeai>=0.8.0
google-genai>=1.0.0

# === CORE UTILS ===
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# === RELIABILITY ===
tenacity>=9.0.0
typing-extensions>=4.10.0
orjson>=3.9.0
